import {createClient} from "@/lib/supabase/server";export async function getUser(){const s=await createClient();return(await s.auth.getUser()).data.user}export async function requireAdmin(){const user=await getUser();if(!user)throw new Error("UNAUTHENTICATED");const s=await createClient();const{data:p}=await s.from("profiles").select("role").eq("id",user.id).single();if(!p||!["admin","editor"].includes(p.role))throw new Error("FORBIDDEN");return{user,profile:p}}