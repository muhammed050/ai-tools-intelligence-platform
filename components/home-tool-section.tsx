import Link from 'next/link'
import { ArrowRight, CheckCircle2 } from 'lucide-react'
import { FavoriteButton } from '@/components/favorite-button'

type Tool = { id: string; name: string; slug: string; short_description: string; rating: number | null; review_count: number; pricing_type: string | null; logo_url: string | null; verified: boolean; category: { name: string; slug: string } | { name: string; slug: string }[] | null }
export function HomeToolSection({ eyebrow, title, tools }: { eyebrow: string; title: string; tools: Tool[] }) { if (!tools.length) return null; return <section className="container" style={{ padding: '54px 0 0' }}><div className="section-head"><div><div className="eyebrow">{eyebrow}</div><h2>{title}</h2></div><Link className="btn btn-secondary" href="/tools">View directory <ArrowRight size={15} /></Link></div><div className="tool-grid">{tools.map((tool) => { const category = Array.isArray(tool.category) ? tool.category[0] : tool.category; return <article className="card tool-card" key={tool.id}><Link href={`/tools/${tool.slug}`} className="tool-card-link"><div style={{ display: 'flex', justifyContent: 'space-between', gap: 10 }}><div className="tool-logo">{tool.logo_url ? <img src={tool.logo_url} alt={`${tool.name} logo`} width={32} height={32} /> : tool.name.slice(0, 1)}</div>{tool.verified && <span className="badge"><CheckCircle2 size={13} /> Verified</span>}</div><div className="muted" style={{ marginTop: 15, fontSize: 12 }}>{category?.name || 'AI software'}</div><h3 style={{ margin: '4px 0' }}>{tool.name}</h3><p className="muted" style={{ margin: 0 }}>{tool.short_description}</p><div style={{ display: 'flex', gap: 8, alignItems: 'center', marginTop: 'auto', paddingTop: 16, fontSize: 13 }}><span>★ {tool.rating ?? 'Unrated'} {tool.review_count ? `(${tool.review_count})` : ''}</span><span className="badge">{tool.pricing_type || 'Pricing unknown'}</span></div></Link><div className="tool-card-actions"><FavoriteButton toolId={tool.id} /><Link className="btn btn-primary" href={`/tools/${tool.slug}`}>View details <ArrowRight size={14} /></Link></div></article> })}</div></section> }
