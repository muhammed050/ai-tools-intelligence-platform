import { redirect } from 'next/navigation'
import Link from 'next/link'
import { requireAdmin } from '@/lib/auth'
import { Users, Wrench, BarChart3, Plus } from 'lucide-react'
export default async function Admin(){try{await requireAdmin()}catch{redirect('/dashboard')}return <main className="container" style={{padding:'54px 0'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:20,flexWrap:'wrap'}}><div><span className="muted">Control center</span><h1 style={{fontSize:38,margin:'8px 0'}}>Admin Dashboard</h1><p className="muted">Manage users, tools and platform operations.</p></div><Link className="btn btn-primary" href="/admin/tools/new"><Plus size={17}/> Add Tool</Link></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(220px,1fr))',gap:16,marginTop:28}}><Link className="card" href="/admin/tools" style={{padding:24}}><Wrench size={24}/><h2>Tools</h2><p className="muted">Create, verify, publish and maintain the AI catalog.</p></Link><Link className="card" href="/admin/users" style={{padding:24}}><Users size={24}/><h2>Users</h2><p className="muted">Review accounts and manage roles securely.</p></Link><Link className="card" href="/admin/analytics" style={{padding:24}}><BarChart3 size={24}/><h2>Analytics</h2><p className="muted">Review platform activity and conversion data.</p></Link></div></main>}
