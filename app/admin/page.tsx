import Link from"next/link";import{redirect}from"next/navigation";import{requireAdmin}from"@/lib/auth";import{createClient}from"@/lib/supabase/server";export default async function Admin(){try{await requireAdmin()}catch{redirect("/auth/sign-in")}const db=await createClient();const[{count:tools},{count:reviews},{count:users}]=await Promise.all([db.from("tools").select("*",{count:"exact",head:true}),db.from("reviews").select("*",{count:"exact",head:true}),db.from("profiles").select("*",{count:"exact",head:true})]);return <main className="container" style={{padding:"55px 0"}}><div style={{display:"flex",justifyContent:"space-between"}}><h1>Admin Dashboard</h1><Link className="btn btn-primary" href="/admin/tools/new">+ Add tool</Link></div><div style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:16,marginTop:28}}>{[["Tools",tools],["Reviews",reviews],["Users",users]].map(([a,b])=><div className="card" style={{padding:24}} key={String(a)}><div className="muted">{a}</div><div style={{fontSize:34,fontWeight:900}}>{b??0}</div></div>)}</div><div className="card" style={{padding:24,marginTop:20}}><Link href="/admin/tools">Manage tools →</Link></div></main>}