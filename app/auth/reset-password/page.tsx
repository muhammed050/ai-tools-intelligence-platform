'use client'
import { createClient } from '@/lib/supabase/browser'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
export default function ResetPassword(){const[password,setPassword]=useState('');const[confirm,setConfirm]=useState('');const[error,setError]=useState('');const[success,setSuccess]=useState('');const[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setError('');setSuccess('');if(password.length<8){setError('Password must be at least 8 characters.');return}if(password!==confirm){setError('Passwords do not match.');return}setLoading(true);const{error}=await createClient().auth.updateUser({password});setLoading(false);if(error){setError('This reset link is invalid or expired. Request a new one.');return}setSuccess('Password updated successfully.');setTimeout(()=>router.push('/dashboard'),700)}return <main className="container" style={{minHeight:'calc(100vh - 68px)',display:'grid',placeItems:'center',padding:'48px 0'}}><section className="card" style={{width:'min(100%,480px)',padding:32}}><span className="muted">Secure password reset</span><h1 style={{fontSize:32}}>Choose a new password</h1><form onSubmit={submit} style={{display:'grid',gap:16,marginTop:24}}><label>New password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password" required minLength={8}/></label><label>Confirm password<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} autoComplete="new-password" required/></label>{error&&<p role="alert" style={{color:'#fb7185'}}>{error}</p>}{success&&<p role="status" style={{color:'#4ade80'}}>{success}</p>}<button className="btn btn-primary" disabled={loading}>{loading?'Updating password...':'Update password'}</button></form></section></main>}
