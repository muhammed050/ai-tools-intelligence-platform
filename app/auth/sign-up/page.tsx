 "use client";import{createClient}from"@/lib/supabase/browser";import{useState}from"react";import Link from"next/link";export default function SignUp(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const{error}=await createClient().auth.signUp({email,password});setMsg(error?error.message:"Account created. Check your email if confirmation is enabled.")}return <main className="container" style={{padding:"90px 0"}}><form onSubmit={submit} className="card" style={{maxWidth:460,margin:"auto",padding:28}}><h1>Create account</h1><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><br/><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={8} required/></label><br/><button className="btn btn-primary" style={{width:"100%"}}>Create account</button><p className="muted">{msg}</p><Link href="/auth/sign-in">Back to sign in</Link></form></main>}