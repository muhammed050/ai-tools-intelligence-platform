 "use client";import{createClient}from "@/lib/supabase/browser";import{useState}from"react";import Link from"next/link";import{useRouter}from"next/navigation";export default function SignIn(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const{error}=await createClient().auth.signInWithPassword({email,password});if(error)setError(error.message);else router.push("/dashboard")}return <main className="container" style={{padding:"90px 0"}}><form onSubmit={submit} className="card" style={{maxWidth:460,margin:"auto",padding:28}}><h1>Welcome back</h1><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><br/><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></label><br/>{error&&<p style={{color:"#fb7185"}}>{error}</p>}<button className="btn btn-primary" style={{width:"100%"}}>Sign in</button><p className="muted">No account? <Link href="/auth/sign-up">Create one</Link></p></form></main>}