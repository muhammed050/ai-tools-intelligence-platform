'use client'
import { createClient } from '@/lib/supabase/browser'
import { useState } from 'react'
import Link from 'next/link'
export default function ForgotPassword(){const[email,setEmail]=useState('');const[error,setError]=useState('');const[success,setSuccess]=useState('');const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError('');setSuccess('');setLoading(true);const{error}=await createClient().auth.resetPasswordForEmail(email.trim(),{redirectTo:`${window.location.origin}/auth/reset-password`});setLoading(false);if(error)setError('Unable to send the reset email. Please try again.');else setSuccess('If an account exists for this email, a password reset link has been sent.')}return <main className="container" style={{minHeight:'calc(100vh - 68px)',display:'grid',placeItems:'center',padding:'48px 0'}}><section className="card" style={{width:'min(100%,480px)',padding:32}}><span className="muted">Account recovery</span><h1 style={{fontSize:32}}>Forgot your password?</h1><p className="muted">Enter your email and we’ll send you a secure reset link.</p><form onSubmit={submit} style={{display:'grid',gap:16,marginTop:24}}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" required /></label>{error&&<p role="alert" style={{color:'#fb7185'}}>{error}</p>}{success&&<p role="status" style={{color:'#4ade80'}}>{success}</p>}<button className="btn btn-primary" disabled={loading}>{loading?'Sending...':'Send reset link'}</button></form><p className="muted" style={{marginTop:20}}><Link href="/auth/sign-in">Back to sign in</Link></p></section></main>}
