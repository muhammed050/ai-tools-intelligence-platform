import{redirect}from"next/navigation";import{getUser}from"@/lib/auth";export default async function Dashboard(){const user=await getUser();if(!user)redirect("/auth/sign-in");return <main className="container" style={{padding:"60px 0"}}><h1>Your dashboard</h1><p className="muted">{user.email}</p><div className="card" style={{padding:24,marginTop:20}}>Saved tools, comparisons, alerts and reviews.</div></main>}