import { redirect } from 'next/navigation'
import Link from 'next/link'
import { getProfile, getUser } from '@/lib/auth'
import { Heart, GitCompare, Search, Star, Bell, UserCircle } from 'lucide-react'

const items=[['Saved Tools','/dashboard/favorites',Heart],['Comparisons','/compare',GitCompare],['Search History','/dashboard/search-history',Search],['Reviews','/dashboard/reviews',Star],['Alerts','/dashboard/alerts',Bell],['Profile','/dashboard/profile',UserCircle]] as const
export default async function Dashboard(){const user=await getUser();if(!user)redirect('/auth/sign-in');const profile=await getProfile();return <main className="container" style={{padding:'54px 0'}}><div style={{display:'flex',justifyContent:'space-between',gap:20,alignItems:'end',flexWrap:'wrap'}}><div><span className="muted">Your workspace</span><h1 style={{fontSize:38,margin:'8px 0'}}>Welcome{profile?.full_name?`, ${profile.full_name}`:''}</h1><p className="muted">Manage your saved AI discoveries and account settings.</p></div><div className="card" style={{padding:'12px 16px'}}><div className="muted" style={{fontSize:12}}>Signed in as</div><div>{user.email}</div></div></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(220px,1fr))',gap:16,marginTop:28}}>{items.map(([title,href,Icon])=><Link href={href} className="card" key={title} style={{padding:22,display:'flex',gap:15,alignItems:'center',transition:'transform .2s'}}><Icon size={22}/><div><strong>{title}</strong><div className="muted" style={{fontSize:13,marginTop:4}}>Open workspace</div></div></Link>)}</div><section className="card" style={{padding:24,marginTop:24}}><h2 style={{marginTop:0}}>Personalized AI discovery</h2><p className="muted">Use AI Finder to discover tools, then save the ones worth trying. Public browsing never requires an account.</p><Link className="btn btn-primary" href="/ai-finder">Open AI Finder</Link></section></main>}
