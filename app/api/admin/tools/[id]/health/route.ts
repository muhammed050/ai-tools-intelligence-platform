import {NextResponse} from 'next/server';import {requireAdmin} from '@/lib/auth';import {checkToolHealth} from '@/lib/services/monitoring/health';export async function POST(_req:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();return NextResponse.json(await checkToolHealth((await params).id));}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Health check failed'},{status:400});}}