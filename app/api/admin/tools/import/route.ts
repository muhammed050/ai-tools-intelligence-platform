import {NextResponse} from 'next/server';import {z} from 'zod';import {requireAdmin} from '@/lib/auth';import {importPublicTool} from '@/lib/services/importer/public-page';const schema=z.object({url:z.string().url()});export async function POST(req:Request){try{await requireAdmin();const {url}=schema.parse(await req.json());const result=await importPublicTool(url);return NextResponse.json(result);}catch(e){if(e instanceof z.ZodError)return NextResponse.json({error:'Invalid URL'},{status:400});return NextResponse.json({error:e instanceof Error?e.message:'Import failed'},{status:400});}}